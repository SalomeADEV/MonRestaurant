{% extends 'base.html.twig' %}

{% block title %}Mes plats{% endblock %}

{% block body %}

{% for plat in plats %}
 
   <div class="card col-6 mb-3 mx-5" style="max-width: 540px;">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{ asset ('id/category/' ~ plat.id)}}" class="img-fluid rounded-start" alt="{{ plat.libelle }}-id">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{ id.libelle }}</h5>
      </div>
    </div>
  </div>
</div>
{% endblock %}
